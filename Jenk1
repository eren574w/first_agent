pipeline {
    agent none

    stages {
        stage('Checkout') {
            agent any
            steps {
                checkout scm
            }
        }

        stage('List Files') {
            agent any
            steps {
                script {
                    sh 'ls -l'
                }
            }
        }
    }
}
